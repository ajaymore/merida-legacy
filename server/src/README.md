### Google auth secrets

* [Google Developer Console](https://console.developers.google.com/)
* select project, enable google+ api
* Create credentials > OAuth client ID > Web application > provide oauth callback
* Create credentials > OAuth client ID > Other > Download JSON > Read client_id, client_secret, redirect_uri
* Add mobile_client_id to the environment variable
